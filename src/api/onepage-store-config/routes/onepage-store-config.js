'use strict';

/**
 * onepage-store-config router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::onepage-store-config.onepage-store-config');
