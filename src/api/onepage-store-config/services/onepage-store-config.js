'use strict';

/**
 * onepage-store-config service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::onepage-store-config.onepage-store-config');
