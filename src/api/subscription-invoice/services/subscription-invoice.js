'use strict';

/**
 * subscription-invoice service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subscription-invoice.subscription-invoice');
